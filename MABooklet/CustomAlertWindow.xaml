<Window x:Class="MABooklet.CustomAlertWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Uyarı" Height="220" Width="420"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Border CornerRadius="20" Background="#202124" BorderBrush="#DDD" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="5" Opacity="0.2"/>
        </Border.Effect>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="lblTitle" Text="Başlık" FontSize="24" FontWeight="Bold" Foreground="green" HorizontalAlignment="Center"/>

            <TextBlock x:Name="lblMessage" Grid.Row="1" Text="Mesaj..." 
                       TextWrapping="Wrap" VerticalAlignment="Center" HorizontalAlignment="Center" 
                       TextAlignment="Center" FontSize="14" Foreground="white" Margin="0,15"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,0">
                <Button x:Name="btnOk" Content="TAMAM" Width="100" Height="35" Margin="5"
            Style="{StaticResource ModernBtn}" Click="btnOk_Click"/>

                <Button x:Name="btnCancel" Content="İPTAL" Width="100" Height="35" Margin="5"
            Style="{StaticResource ModernBtn}" Click="btnCancel_Click" 
            Visibility="Collapsed" Background="#888"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>